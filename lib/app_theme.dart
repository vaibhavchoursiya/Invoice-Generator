import 'package:flutter/material.dart';

class AppTheme {
  static const Color light = Color.fromARGB(246, 255, 240, 240);
  static const Color dark = Color.fromARGB(234, 22, 22, 22);

  static const Color primary = Colors.indigoAccent;
}
